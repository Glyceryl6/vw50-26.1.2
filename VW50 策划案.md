# VW50 模组 策划案

[TOCM]

[TOC]

# 1.模组基本信息

## 1.1 模组名称

- 中文名：V我50

- 英文名：V me 50

- ID：vw50

## 1.2 模组介绍

- 创作动机：TeaCon2026参赛模组，符合比赛主题1：身边的联系、联结、连接。

- 主题正文：还记得你最初接触到《Minecraft》的契机吗？是身边朋友的推荐带你入坑，还是无意刷到的视频勾起了你的兴趣，或是主动搜索某些关键词给你的深入了解？我们通过形形色色的方式与这款游戏结缘，这些点点滴滴的联系指引你我在此相遇，看似不同的道路却最终将我们连接在一起。
若想以「身边的联系、联结、连接」为主题，能否将现实的连接带入游戏，或者创造一个全新的联系？

- 模组内容：为多人游戏加入了围绕“红包”展开的各种互动玩法。其创作参考了中国流行的，QQ，微信等软件上的红包功能。无论是为了庆祝，还是赠予，或者只是炒热气氛，你可以将你的物品包装成可自定义的红包，发送到公屏聊天栏中，让全服的玩家抢夺你在里面塞的……不知道什么东西。

- 模组简简简介：老板大气！

## 1.3 主要功能

- 发红包：可以将自选的特定数量的特定物品发送到服务器公屏中，发送后会在聊天栏渲染一个特殊的红包组件。如果过期未领取会退还。

- 抢红包：服务器中所有玩家会收到发送的红包，可以在聊天栏中点击抢红包，可能抢到不同数量的红包内容物。

- 自定义红包封面：通过发红包时打开的GUI对红包在聊天栏中展示的组件样式进行一定程度的自定义。可以自定义的部分包括封面（颜色）、标志、标题等。

- 特殊红包属性：是否需要口令领取、是否为专属红包（指定玩家名字）、是否对无关玩家隐藏（玩家不可直接设置）

- 复读玩法：当客户端聊天栏中有两个人说了一样的话时，玩家可以点击出现的+1标识复读上一个人发的相同内容。

- 红包服务器限制：为每个用户发送红包添加冷却（OP无视冷却）。OP可以通过指令提高、限制某个玩家发红包的频率或者禁止某玩家发红包（将限制信息保存到服务端文件中）。

## 1.4 创作方向

- 游玩群体：重视社交性的服务器环境中的玩家。如商业服务器，网友服务器，线上展会服务器，私人服务器等中的玩家。

- 核心想法：让服务器玩家的聊天栏活起来，促进玩家更多使用聊天栏交流。

- 根本氛围：吉祥、热闹、积极、鼓励联系而非孤立、鼓励全员参与。

# 2.核心设计

## 2.1 新增物品

#### 2.1.1 空红包

- ID：vw50:empty_red_envelope

- 中文名：空红包

- 获取方式：无序合成。2纸和1红色染料合成出4个。

- 设计思路：有物品形式的发红包的媒介，比直接指令发红包更让人感到熟悉。

- 特殊效果：如果玩家发送红包的间隔超过了设置值（未处于红包发送冷却期），右键后触发类似纸张的包红包音效，同时打开一个Screen，并将空红包的一些特殊属性传递到待生成的红包中。在这个界面中玩家可以填写红包基本信息并发送红包。发送前需要经过数据校验，发送后创建并序列化一个红包对象，并向全服广播。
如果处于冷却期，则在物品栏上方显示标题：您发送的太快了，请在${time in second}秒后重试！

- 特殊属性：可以设置一个特殊属性IsPrivate，默认为false，如果为true，使用这个空红包创建的红包都是隐藏的。参见章节2.2.4红包设置中关于隐藏的部分。

## 2.2 红包相关

#### 2.2.1 红包对象

- 介绍：红包是一个特殊对象，连接了发送，领取红包的行为，记录了涉及其中的所有相关信息。

- 数据保存：红包在生成时应该序列化并保存到服务端本地指定的红包历史文件。每次发生领取行为时也应该更新序列化。红包领取完成后，理论上不再有保存需求，但依旧可以保存，方便查看红包记录。（如果会显著占用存储空间的话或许可以转移到本地保存？或者定期清除超过8小时的数据？）

- 生命周期：主要通过对储存生命周期的字段值进行修改来展示，在改变同时执行不同的逻辑。

    - 创建：红包通过指令、空红包物品打开的GUI生成。创建后，在所有玩家客户端渲染红包聊天栏组件。

    - 激活：此时无论通过红包聊天栏组件，红包池组件，指令还是红包记录，玩家可以通过领取红包的相关规则领取红包。
    每tick只能处理一个领取请求，同一tick领取的多名玩家只有随机的一名可以领取，其余玩家排队到下一个tick。领取时，将领取的数量和玩家uuid保存到红包信息中，并序列化到服务端，便于后续查询红包领取情况。
    如果正在激活的红包没有红包聊天栏组件，或者红包聊天栏组件没有渲染，那么应该在未领取玩家的客户端渲染红包池组件。

    - 销毁：如果红包已领完，进入销毁流程。所有已存在的红包聊天栏组件将看起来像是已经领完的样子，销毁可能的红包池组件。但序列化到红包历史文件中的内容不应删除。
    如果红包没有领完但是超过了设置的时间限制，则同样会销毁。并且，如果有创建者玩家，则会将剩余的物品返还给创建者。创建者不在线时，在服务器建立一个pending task，订阅玩家登录事件，在创建者登录时返还物品。这个pending task也需要序列化，防止服务器重启时丢失。创建者有可能此后再也不上线，这会导致pending task越堆越多，不过只是检测名字或许不会造成太严重的性能影响？以防万一也可以设计pending task的销毁，比如创建10天后销毁。

- 保存内容：
    - 基础：UUID，标题，标志，皮肤样式（后续内容），包含的ItemStack，生命周期（未创建，激活中，已销毁），是否为系统红包
    - 设置：口令内容，专属用户名，是否隐藏，是否拼手气，过期时是否销毁
    - 领取情况：领取者的用户名（或者uuid），领取数量

- 保存时机：创建时，有人领取时，销毁时

- 不保存的特殊情况：红包为系统红包时不需要保存任何数据。系统红包不保存，不可通过红包历史查询，销毁时不返还物品，服务器重启后随之消失。

#### 2.2.2 红包聊天栏组件

和一个红包对象一一对应。
红包领取后在渲染上做调整，使得玩家能一眼看出来领过了。
红包销毁时不会销毁，而是在渲染上做调整，使得玩家能一眼看出来已经领完了。
会在聊天栏里渲染一个橘红色的长方形，在聊天栏中点击时会执行领取红包的逻辑。
内部左侧是易于辨识的红包图标，可以看出来是红包，往右是自定义标志，再往右是自定义标题。

#### 2.2.3 红包池组件

和一个红包对象一一对应。一个红包没有在聊天栏中渲染时，触发针对未领取用户的在红包池的渲染。红包销毁时，这个组件也会随之销毁。
打开聊天栏时，一个圆形图标在屏幕左侧平滑平移（三角函数）出现。圆形内部是红包图标。随机红包图标向右展开一个长条大R值圆角矩形，矩形内部是自定义标志和标题。圆形图标的外侧用环形白色进度条的方式渲染红包剩余时间。

#### 2.2.4 红包设置

- 是否为口令：默认为false。如果为true，这个红包在发送后将转为监听聊天栏信息，并依据口令内容判断直接跳转到红包领取界面，而常规点击领红包时则不会直接进入红包页面，会在聊天栏组件或者红包池组件的右侧显示一个speech ballon，提示口令内容。设置为true时，需要将口令保存到数据中。

- 口令内容：口令判断的依据。读取数据时，如果存在该项，则将是否为口令设置为true。

- 是否为专属：默认为false。如果为true，只有符合专属用户名信息的玩家才能领取红包。其它人点击组件时右侧显示一个Speech Ballon提示专属红包。如果可以的话，在红包组件里渲染专属用户的头，或者用某些别的方式让专属红包看起来更直观，仅看组件本身不需要额外提示也能知道是专属。设置为true时，需要将专属用户名保存到数据中。

- 专属用户名：专属用户判断的依据。读取数据时，如果存在该项，则将是否为专属设置为true。

- 是否隐藏：默认为false，玩家不可直接设置，只能在通过指令发放红包时用指令设置，或者通过具有特殊属性的红包物品。通过指令生成来自系统的隐藏红包时，隐藏红包必须指定一个或一群可见玩家。通过带属性的特殊空红包生成时，隐藏红包必须是专属红包，且可见玩家就是专属玩家一人。只有隐藏红包的可见玩家可以看到隐藏红包的渲染。

- 是否拼手气：默认为true，决定领取红包时领取到的数量。普通红包尽可能均分物品（必须都是整数，最后总能全部分完），拼手气红包采用“二倍均值”红包算法。

- 过期销毁：默认为false。如果为false，由玩家发出的红包过期销毁时需要返还其中的物品。如果为true，则不会返还。

#### 2.2.5 红包发送页面

红包发送页面包含如下的组件：

1. 标题输入栏：
在GUI最上方的长横输入栏，可以输入红包标题。没有输入任何文字时，在标题输入栏中会提示当前语言的缺省红包标题。举例来说，语言为中文时，缺省红包标题为“恭喜发财”，英文时为“Kung Hei Fat Choy”。

2. 物品栏slot：
在红包的中心位置的黄色方框，可以放置需要发送的物品。放在该栏位中的物品可以超过原版的堆叠限制，以达成发送大数额红包的效果。发送红包时这个栏位必须有物品。

3. 拼手气红包设置checkbox：
在需要发送的物品的**左侧**region中的checkbox，默认为打勾。勾选时红包为拼手气红包，否则为均分的普通红包。在选择框左边渲染“拼手气红包”文字。

4. 过期时退还设置checkbox：
在需要发送的物品的**左侧**region中的checkbox，默认为打勾。勾选时红包过期行为为返还物品，否则销毁物品。在选择框左边渲染“过期时退还”文字。

5. 人数输入栏：
在需要发送的物品的**右侧**region中**左上角**的输入栏，可以设置红包人数。发送红包时这个栏位必须有值，并且不超过需要发送的物品的数量。没有输入任何文字时，会提示“人数”。

6. 属性设置按钮：
在需要发送的物品的**右侧**region中**右上角**的按钮，可以调整发送红包的属性。包含三个值：{“普通”，“口令”，“专属”}，点击时会按顺序切换。默认值为“普通”。这个按钮的值和下方的，序号7对应的 额外设置输入栏 对应。它的设置和额外设置输入栏（下简称“输入栏”）的对应有如下的交互规则。
    1. 每次切换这个按钮的值时，输入栏中的文字全部清除。此举是为了防止玩家误操作，将用户名作为口令或者将口令作为用户名发出。

    2. 当值为“普通”时，不渲染输入栏，且无法选中并在输入栏中输入。

    3. 当值为“口令”时，输入栏中输入的内容为红包口令。没有输入任何文字时，会提示“红包口令”。

    4. 当值为“专属”时，输入栏中输入的内容为红包专属用户名。没有输入任何文字时，会提示“专属玩家”。

7. 额外设置输入栏：
在需要发送的物品的**右侧**region中**下方**的输入栏，需要输入的内容取决于上方的，序号6对应的 属性设置按钮 的值。在上方按钮设置为“普通”时不渲染且无法选中并输入。具体和按钮的对应规则见上条。

8. 红包预览组件：
在GUI的左下角。根据红包的标题，标志和皮肤实时预览发送出红包的组件的样子。应该真实呈现出红包发送到聊天栏时的红包聊天栏组件应有的样子。
*在预览组件中还应该包含一个标志选择按钮，点击时可以展开红包标志选择菜单，更换红包标志。具体行为待红包标志系统完善后补充。*

9. 发送按钮：
在GUI的右下角。如果序号2 物品栏内有物品&&序号5 人数输入栏填写了数字，那么发送按钮为可用状态。否则该按钮标记为不可用。点击后开始校验输入值的合理性，消耗空红包物品并处理发送红包的逻辑。红包无法通过校验时，在发送按钮下方输出错误信息。

红包发送页面需要玩家输入基本的红包信息并校验输入信息才能输入红包。红包发送时，需要对红包属性做几个合理性校验和自动纠错。没有通过合理性校验的红包会拒绝发放，并在发送按钮下方输出错误信息。

校验：

1. 非空校验：必须存在需要发送的物品，必须填写红包人数。错误信息：发送物品为空/未填写人数。一般情况下，由于发送按钮会提前校验这两个栏位，这个校验总会通过。

2. 发送物品校验：红包发送的物品不能是管理员物品和配置中黑名单设置的物品。这一项校验也可以通过配置开关。错误信息：物品被Ban。

3. 数字校验：人数输入栏中的值必须是小于256的合法整数。错误信息：人数错误。

4. 人数校验：设置领取红包人数不能超过物品总数量。错误信息：领取人数多于物品总数。

5. 标题长度校验：标题不得超过20字符。错误信息：标题过长。

6. 专属用户校验：发送专属红包时，专属用户必须在线。错误信息：专属用户不在线。

7. 口令长度校验：口令不得超过30字符。错误信息：口令过长。

8. 口令合法性校验：口令必须是可以正常输出的，不会触发任何可能的注入错误的字符串（具体什么是注入错误，这个范围需要根据实际业务中遭遇到的bug来拓展）。比如，口令必须不能以“/”字符开头，避免被系统识别为指令。

纠错：

1. 标题纠错：如果发送红包时没有填写红包标题，则会以当前语言的缺省红包标题作为红包名发送。

#### 2.2.6 领红包

非口令红包可以直接通过点击组件进入领红包流程。该流程会执行抢红包的逻辑，触发一次红包的序列化，并打开这个红包详细领取历史的screen。如果可以的话我希望这个screen有动态进入效果，比如淡入或者从下往上滑出。如果已经领取过，就会直接打开红包详细领取记录。

## 2.3 新增指令

#### 2.3.1 /vw50 hand

权限等级：1

语法：
/vw50 hand \<playerCount> [label]

将自己手上的东西作为红包发送。人数为playerCount。label为字符串可选项，代表红包标题。没有默认为当前语言的缺省红包标题。
这条指令会受到一般的发送红包时间限制。

#### 2.3.2 /vw50 envelope

权限等级：2

简单语法：
/vw50 envelope \<label> \<item> \<stackCount> \<playerCount>

创建一个新红包，使用指定的label，item，stackCount和playerCount。
这条指令会受到一般的发送红包时间限制。

完整语法：
/vw50 envelope \<label> \<item> \<stackCount> \<playerCount> \<sign> \<EnvelopeProperty> \<ignoreLimit>

创建一个新红包，使用指定的label，item，stackCount，playerCount和sign。同时需要传入EnvelopeProperty（一个包括专属玩家名ExclusivePlayerName，是否销毁DestroyWhenExpired，是否隐藏IsHidden，口令Password的property记录对象）
这条指令是否受到一般的发送红包时间限制取决于ignoreLimit值。

#### 2.3.3 /vw50 permission

#### 2.3.4 /vw50 history

#### 2.3.5 /vw50 system_envelope


拓展：
收款形式？付费开启的红包？可以发送多组内容的红包？接龙红包？送礼？红包皮肤？